# === Local Voice Agent (.env) ===
# Copy this to .env and fill in your Neon Postgres URL.
# Example (with sslmode=require):
# postgresql+psycopg2://<user>:<password>@<host>/<db>?sslmode=require

DATABASE_URL=postgresql+psycopg2://neondb_owner:npg_ryB1npPiv8bo@ep-noisy-morning-a4wbxrmk-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Optional: default phone number to match bookings when user says "my booking"
DEFAULT_CALLER_PHONE=+919633717592
